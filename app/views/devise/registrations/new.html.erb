<div class="row">
  <div class="col-lg-4 col-md-6 ml-auto mr-auto">
    <h1 class="text-center"><%= t('.sign_up') %></h1>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.email_field :email, autofocus: false, class: 'form-control', placeholder: "Email Address" %>
      </div>

      <div class="form-group">
        <%= f.text_field :username, autofocus: false, class: 'form-control', placeholder: "Nickname del Torneo" %>
      </div>

      <div class="form-group">
        <%= f.text_field :first_name, autofocus: false, class: 'form-control', placeholder: "Nome" %>
      </div>

      <div class="form-group">
        <%= f.text_field :last_name, autofocus: false, class: 'form-control', placeholder: "Cognome" %>
      </div>

      <div class="form-group">
        <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Password' %>
      </div>

      <div class="form-group">
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', placeholder: 'Confirm Password' %>
      </div>

      <div class="form-group">
        <%= invisible_recaptcha_tags text: t('.sign_up'), class: "btn btn-primary btn-block btn-lg" %>
      </div>
    <% end %>

    <div class="text-center">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
